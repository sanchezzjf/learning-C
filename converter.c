#include <stdlib.h>
#include <stdio.h>

int hexToDecimal(int num);
int decToHex(int num);

int main(void){
    uint8_t num = 100;
    printf("%d", num);
}

int hexToDecimal(int num){}

int decToHex(int num){}